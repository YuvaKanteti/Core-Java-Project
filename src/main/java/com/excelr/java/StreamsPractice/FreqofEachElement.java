package com.excelr.java.StreamsPractice;

import java.util.Arrays;
import java.util.List;

public class FreqofEachElement{
	public static void main(String[] args) {
		List <String> string = Arrays.asList("Yuva","Vasu","Prakash");
		
		List <Integer> Freq = numbers.stream()
				                      .map(n->string::comparator.frequency(n,freq)_))
				                      .filter(n->(n%2==0))
				                      colect(Collectors.toList());
		System.out.println();

}
	

}
